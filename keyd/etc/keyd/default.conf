# keyd configuration
# system-wide key remapping daemon
# see: https://github.com/rvaiya/keyd

[ids]
*

[main]
# caps lock: esc on tap, ctrl on hold (200ms timeout)
capslock = overloadt(control, esc, 200)
